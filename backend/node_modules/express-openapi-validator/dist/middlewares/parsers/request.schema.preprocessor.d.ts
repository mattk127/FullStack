import ajv = require('ajv');
import { OpenAPIV3 } from '../../framework/types';
export declare class RequestSchemaPreprocessor {
    private ajv;
    private apiDoc;
    constructor(apiDoc: OpenAPIV3.Document, options: ajv.Options);
    preProcess(): void;
    private preprocessRequestBody;
    private preprocessPathLevelParameters;
    private cleanseContentSchema;
    private handleDiscriminator;
    private discriminatorTraverse;
    private traverse;
    private findKey;
    getKeyFromRef(ref: any): any;
}
